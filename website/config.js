
var config = {};

config.hostname = 'imageframe';
config.home = '/home/' + config.hostname;
config.scriptPath = config.home + '/scripts/';
config.imagePath = config.home + '/images/tmp';

module.exports = config;
